// src/contracts.ts (CORRECT)
import UserIdentityABI from '../abis/UserIdentity';

// IMPORTANT: Replace this with the actual address after you deploy!
export const userIdentityAddress = '0xd1b04ac2e727f76239e3173c4f2d105cc55a3eff';

// The imported UserIdentityABI is already the ABI array, so we just export it directly.
export const userIdentityABI = UserIdentityABI;